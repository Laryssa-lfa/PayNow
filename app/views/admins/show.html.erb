<div class="container">
  <h4><%= @admin.name %></h4>
  <h4><%= @admin.email %></h4>
  <h4><%= @admin.occupation %></h4>
  <br>

  <%= link_to 'Editar', admin_path(@admin), class: "btn btn-dark" %>
  <br>
  <%= link_to 'Apagar', admin_path(@admin), method: :delete, class: "btn btn-dark" %>
  <br>
  <%= link_to 'Voltar', admins_path, class: "btn btn-dark" %>
</div>
